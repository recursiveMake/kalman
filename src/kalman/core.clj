(ns kalman.core
  (:require [kalman.filter :refer [kalman, logging-kalman]]
            [kalman.sample :as sample]
            [kalman.visual :as visual])
  (:gen-class))

(defn -main
  ""
  [& args]
  )
